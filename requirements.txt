numpy>=1.24
scikit-learn>=1.3
pytest>=7.4

# Optional extras -- uncomment (or `pip install -e ".[openai]"` etc.) to enable:
# openai>=1.0                  # LLM_PROVIDER=openai / EMBEDDER=openai
# anthropic>=0.25              # LLM_PROVIDER=anthropic
# sentence-transformers>=2.2   # EMBEDDER=sbert
# fastapi>=0.110                # HTTP service (src/fashion_rag/api.py) -- see infra/
# uvicorn[standard]>=0.29       # ASGI server for the API, also `pip install -e ".[api]"`
# httpx>=0.27                   # needed by fastapi.testclient in tests/test_api.py
